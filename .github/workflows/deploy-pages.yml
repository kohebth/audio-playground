name: Deploy Audio Playground

on:
  push:
    tags:
      - 'v2.0-beta[0-9]+'
  pull_request:
    branches:
      - main
    paths:
      - ".github/workflows/deploy-pages.yml"
      - "CMakeLists.txt"
      - "cmake/**"
      - "../../apg-core/include/**"
      - "apg-core/src/**"
      - "schema/**"
      - "test/fixtures/**"
      - "test/golden/**"
      - "codegen/**"
      - "apg-wasm/**"
      - "apg-web/**"
permissions:
  contents: read

concurrency:
  group: pages-${{ github.event_name == 'push' && 'production' || github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: Build and validate
    runs-on: ubuntu-24.04

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Configure Pages
        uses: actions/configure-pages@v6

      - name: Set up Node
        uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: |
            apg-wasm/package-lock.json
            apg-web/package-lock.json

      - name: Set up Emscripten
        uses: emscripten-core/setup-emsdk@v16
        with:
          version: 5.0.1
          actions-cache-folder: emsdk-cache

      - name: Install WASM facade dependencies
        working-directory: apg-wasm
        run: npm ci

      - name: Build WASM facade
        working-directory: apg-wasm
        run: npm run build

      - name: Build and smoke-test WASM runtime
        run: |
          emcmake cmake -S . -B build/wasm -DCMAKE_BUILD_TYPE=Release
          cmake --build build/wasm --target apg_control apg_processor -j2
          node apg-wasm/test/emscripten_runtime_smoke.mjs build/wasm/apg-wasm .
          mkdir -p apg-web/public/wasm
          cp build/wasm/apg-wasm/apg_control.mjs build/wasm/apg-wasm/apg_control.wasm apg-web/public/wasm/
          cp build/wasm/apg-wasm/apg_processor.mjs build/wasm/apg-wasm/apg_processor.wasm apg-web/public/wasm/
          cp apg-wasm/web/processor.worklet.js apg-web/public/wasm/processor.worklet.js

      - name: Install web dependencies
        working-directory: apg-web
        run: npm ci

      - name: Typecheck
        working-directory: apg-web
        run: npm run typecheck

      - name: Lint
        working-directory: apg-web
        run: npm run lint

      - name: Test
        working-directory: apg-web
        run: npm test

      - name: Build Pages application
        working-directory: apg-web
        env:
          VITE_BASE_PATH: /audio-playground/
          VITE_COMMIT_SHA: ${{ github.sha }}
        run: npm run build

      - name: Disable Jekyll processing
        run: touch apg-web/dist/.nojekyll

      - name: Validate Pages artifact
        working-directory: apg-web
        env:
          VITE_BASE_PATH: /audio-playground/
        run: npm run pages:validate

      - name: Install Chromium
        working-directory: apg-web
        run: npx playwright install --with-deps chromium

      - name: Run Pages smoke tests
        working-directory: apg-web
        env:
          APG_EXPECTED_COMMIT_SHA: ${{ github.sha }}
        run: npm run pages:smoke

      - name: Upload Pages artifact
        if: github.event_name == 'push' && github.ref_type == 'tag'
        uses: actions/upload-pages-artifact@v5
        with:
          path: apg-web/dist

  deploy:
    name: Deploy
    if: github.event_name == 'push' && github.ref_type == 'tag'
    needs: build
    runs-on: ubuntu-24.04

    permissions:
      pages: write
      id-token: write

    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
