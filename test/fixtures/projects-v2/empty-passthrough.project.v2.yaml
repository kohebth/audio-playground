kind: apg.project
schema: apg.project.v2
name: empty-passthrough
version: 2.0.0

units: []

chain:
  nodes: []
  routes:
    - from: system.input
      to: system.output

targets:
  default: desktop_full
  export:
    - wasm_realtime
    - offline_render
